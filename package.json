{
  "name": "okomix",
  "version": "1.0.0",
  "description": "",
  "main": "tailwind.config.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build:css": "tailwindcss -i css/tailwind-input.css -o dist/css/_tw.css --minify && cat dist/css/_tw.css css/styles.css > dist/css/styles.min.css && rm dist/css/_tw.css",
    "build:js": "esbuild js/entry.js --bundle --minify --target=es2020 --outfile=dist/js/bundle.min.js",
    "build:html": "node scripts/build-html.js",
    "build:assets": "cp -r images dist/images && cp CNAME dist/",
    "build": "rm -rf dist && mkdir -p dist/css dist/js && npm run build:css && npm run build:js && npm run build:html && npm run build:assets",
    "dev": "tailwindcss -i css/tailwind-input.css -o css/tailwind.css --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gbaygon/okomix.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/gbaygon/okomix/issues"
  },
  "homepage": "https://github.com/gbaygon/okomix#readme",
  "devDependencies": {
    "esbuild": "^0.24.0",
    "tailwindcss": "^3.4.19"
  },
  "dependencies": {
    "alpinejs": "^3.14.0",
    "aos": "^2.3.4"
  }
}
